[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lunar-image-registration"
version = "0.1.0"
description = "Classical baseline for lunar image registration"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]